Error-Estimate reason for:
(Because hK≤hmax, this formula immediately implies the previous one if you just pull the mesh size out of the sum.) What this formula suggests is that it is not necessary to make the largest cell small, but that the cells really only need to be small where ∥∇p+1u∥K is large! In other words: The mesh really only has to be fine where the solution has large variations, as indicated by the p+1st derivative. This makes intuitive sense: if, for example, we use a linear element p=1, then places where the solution is nearly linear (as indicated by ∇2u being small) will be well resolved even if the mesh is coarse. Only those places where the second derivative is large will be poorly resolved by large elements, and consequently that's where we should make the mesh small